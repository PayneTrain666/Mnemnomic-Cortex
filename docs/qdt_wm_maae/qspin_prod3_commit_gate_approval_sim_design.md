# Commit-Gate Approval Simulation Design

The approval simulator approves active-dry-run only. It blocks unsafe flags, writes, raw traces, commit execution, production activation, missing source matrix, missing rollback evidence, and disabled/tripped kill-switch states.
