# Backup/Recovery and Migration Dry-Run Planning

`BackupRecoveryPlanner` creates required backup/restore review steps without executing backup or restore. `MigrationDryRunPlanner` creates forward/rollback migration simulation steps without executing schema migrations.
