# REASON-3C Controller Planner Integration

`ControllerPlannerIntegration` is explicitly opt-in. It runs planner, evaluation, failure classification, remediation guidance, and quality hardening in a bounded metadata path. `write_permission=True` is rejected.
