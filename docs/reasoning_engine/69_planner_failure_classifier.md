# REASON-3B Planner Failure Classifier

The classifier converts evaluation signals into bounded `PlannerFailureRecord` objects with deterministic IDs. It is disabled by default and performs no patching or memory-store mutation.
