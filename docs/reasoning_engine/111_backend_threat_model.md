# Backend Threat Model

`BackendThreatModelBuilder` covers credentials, integrity, availability, privacy/redaction, and migration risk. All findings are JSON-safe metadata and block write-capable code until resolved.
