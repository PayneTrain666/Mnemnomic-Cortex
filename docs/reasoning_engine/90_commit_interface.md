# REASON-4A Commit Interface

`ReasoningCommitInterface` validates explicit commit requests and returns decision metadata. Even approved explicit commit intent is metadata-only and performs no real write.
