# Credential Scope Model

`CredentialScopeModel` allows disabled or test-injected metadata scopes only. Real secret loading, production secret-manager access, write credentials, schema migration credentials, admin credentials, and credential logging are blocked.
