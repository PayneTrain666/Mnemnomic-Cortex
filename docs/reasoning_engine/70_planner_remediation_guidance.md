# REASON-3B Planner Remediation Guidance

The remediation layer is recommendation-only. It blocks automatic memory-store writes, model/optimizer mutation, policy/component activation, real ablation execution, destructive replacement, and automatic patch application.
