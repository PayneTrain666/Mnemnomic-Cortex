# Backend Implementation Plan

`BackendImplementationPlanner` proposes JSONL, SQLite, Postgres, and vector-store options. JSONL and SQLite are conservative first options. The plan remains no-write and requires a separate authorization command before write-capable code.
